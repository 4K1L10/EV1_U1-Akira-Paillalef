@model IEnumerable<MercyDevelopers.Models.ClienteHasServicio>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdClienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdServiciosNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdClienteNavigation.IdCliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdServiciosNavigation.IdServicios)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdClienteHasServicios">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdClienteHasServicios">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdClienteHasServicios">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
